/**
 * Napisz program, który działa zgodnie z nastepujaca zasada:
 * • program odbiera ramka sieci Ethernet II z interfejsu sieciowego, którego nazwa podawana
 * jest jako jego pierwszy argument wywołania;
 * • program, odebrane z tej ramki dane, przesyła do kolejnego odbiorcy, korzystajac z tego samego interfejsu sieciowego,
 * pod adres MAC, który podawany jest jako jego drugi argument wywołania;
 * • program powraca do odbierania kolejnej ramki.
 * Jako wartosci EtherType uzyj liczby 0x8888.
**/